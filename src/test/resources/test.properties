# OrangeHRM Test Configuration
app.url=https://opensource-demo.orangehrmlive.com/web/index.php/auth/login
app.username=Admin
app.password=admin123

# Timeouts (in milliseconds) - optimized for fastest page loads
page.load.timeout=15000
element.wait.timeout=5000

# Network optimization for external sites
# Reduced timeout prevents hanging on slow sites
# LOAD wait condition is fastest (waits only for page load event)
navigation.timeout=15000
wait.until.condition=LOAD